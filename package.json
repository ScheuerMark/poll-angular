{
  "name": "poll",
  "version": "1.0.0",
  "engines": {
    "node": ">=18 <19"
  },
  "scripts": {
    "start": "cd express-backend/ && npm start",
    "prebuild": "cd express-backend/ && npm run build",
    "build": "cd angular-poll/ && npm run build"
  }
}
